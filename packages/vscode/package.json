{
  "name": "rifen-timescope",
  "displayName": "TimeScope",
  "description": "Hover any integer duration → see human-readable time instantly. TimeScope converts raw numbers into meaningful durations for VS Code.",
  "version": "0.2.25",
  "publisher": "rifen",
  "engines": {
    "vscode": "^1.136.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "timeout",
    "duration",
    "hover",
    "timestamp",
    "productivity"
  ],
  "activationEvents": [
    "onLanguage:plaintext",
    "onLanguage:javascript",
    "onLanguage:typescript",
    "onLanguage:python",
    "onLanguage:go",
    "onLanguage:yaml",
    "onLanguage:json",
    "onLanguage:toml",
    "onLanguage:dockerfile",
    "onLanguage:makefile"
  ],
  "main": "./dist/extension.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rifen/timescope.git"
  },
  "bugs": {
    "url": "https://github.com/rifen/timescope/issues"
  },
  "homepage": "https://github.com/rifen/timescope#readme",
  "vsce": {
    "dependencies": false
  },
  "dependencies": {
    "@rifen/timescope-core": "workspace:*"
  },
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "TimeScope",
      "properties": {
        "timescope.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable/disable TimeScope hover provider"
        },
        "timescope.defaultUnit": {
          "type": "string",
          "enum": [
            "seconds",
            "milliseconds",
            "microseconds",
            "nanoseconds",
            "hours",
            "days",
            "weeks",
            "months",
            "years",
            "auto"
          ],
          "default": "seconds",
          "description": "Default unit to assume when ambiguous"
        },
        "timescope.format": {
          "type": "string",
          "enum": [
            "compact",
            "verbose",
            "both"
          ],
          "default": "compact",
          "description": "Output format for durations"
        },
        "timescope.minValue": {
          "type": "number",
          "default": 1,
          "description": "Ignore values below this threshold"
        },
        "timescope.maxValue": {
          "type": "number",
          "default": 31557600000,
          "description": "Ignore values above this threshold (~1000 years in ms)"
        },
        "timescope.contextClues": {
          "type": "boolean",
          "default": true,
          "description": "Use var names, comments, file type to infer unit"
        },
        "timescope.fileTypes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "*"
          ],
          "description": "Glob patterns for file types to activate on"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "build": "npm run compile && node ./scripts/build-bundle.cjs",
    "vscode:prepublish": "echo 'Skipping prepublish - build already run in CI'",
    "pretest": "npm run compile && npm run lint",
    "test": "node ./test/runTest.js",
    "unit-test": "mocha --require ts-node/register test/*.test.ts",
    "lint": "eslint src --ext .ts --ignore-path .gitignore",
    "package": "@vscode/vsce package --no-dependencies",
    "publish": "@vscode/vsce publish --no-dependencies"
  },
  "commands": [
    {
      "command": "timescope.toggle",
      "title": "TimeScope: Toggle Enabled",
      "category": "TimeScope"
    },
    {
      "command": "timescope.dumpSettings",
      "title": "TimeScope: Dump Settings to Output",
      "category": "TimeScope"
    },
    {
      "command": "timescope.logHoverTarget",
      "title": "TimeScope: Log Hover Target",
      "category": "TimeScope"
    }
  ],
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.136.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "@vscode/test-electron": "^2.3.8",
    "@vscode/vsce": "^3.0.0",
    "eslint": "^8.56.0",
    "glob": "^10.3.10",
    "mocha": "^11.8.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "icon": "extension/timescope.ico"
}