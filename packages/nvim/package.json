{
  "name": "timelens-nvim",
  "version": "0.2.5",
  "description": "Neovim plugin for TimeScope - hover to see human-readable durations",
  "main": "lua/timelens/init.lua",
  "scripts": {
    "bridge": "node bin/bridge.js",
    "build": "tsc -p tsconfig.bridge.json",
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@rifen/timescope-core": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.12",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "neovim": ">=0.5.0",
    "node": ">=18.0.0"
  }
}
